NODE_ENV=development
DATABASE_URL=postgresql://user:password@localhost:5432/omni_dev

# JWT_SECRET must be a cryptographically secure random string (minimum 32 characters)
# Generate with: openssl rand -hex 32
# WARNING: Never use default values in production!
JWT_SECRET=

PORT=3000
